services:
  healthcare-capacity-dashboard:
    build:
      context: .
    image: healthcare-capacity-dashboard:local
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      STREAMLIT_SERVER_HEADLESS: "true"
      STREAMLIT_BROWSER_GATHER_USAGE_STATS: "false"
